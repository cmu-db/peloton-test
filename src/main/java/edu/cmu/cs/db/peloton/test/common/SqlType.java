package edu.cmu.cs.db.peloton.test.common;

/**
 * Represents types of values allowed in SQL
 */
public enum SqlType {
    // TODO: Add all SQL types
    // Note, enum names should be the same as string representation
    // of the type in the database
    INT, VARCHAR, BOOLEAN, REAL
}
